package levels.tiles;

/**
 * Class that represents an empty tile.
 * Used to specify that the tile wont have any collision factor.
 * @author sergio
 */
public class EmptyTile extends Tile {

    public EmptyTile(int x, int y) {
        super(x,y);
    }
}
